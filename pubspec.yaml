name: action_button
description: >
  A production-ready async action button abstraction for Flutter.
  Standardizes async button state handling (idle, loading, success, error) with a builder pattern.
version: 0.0.1
homepage: https://github.com/NikhilD1998/action_button
repository: https://github.com/NikhilD1998/action_button
issue_tracker: https://github.com/NikhilD1998/action_button/issues
documentation: https://pub.dev/documentation/action_button/latest/
topics:
  - button
  - async
  - state-management
  - flutter

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # No assets or fonts by default
